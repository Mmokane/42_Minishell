what to do in this project : 

parsing :

-fisrt : the lexer , The lexer takes the command line and parses it into tokens, 
		 the tokens are two types, WORD (null, ok, file...), and Speacial characters (",', < . << , $ , spaces, >>>), 
li fhmt : lexer hwa whd tool li ktakhd lina l input mn terminal o ktrj3o tokens, y3ni words with meaning, like n3tiwha 'hey' , ayrj3 lina ' then hey then '.


kshni nsali expand dzp 3ad nzid 
parts : lexer / is doone;
		tokens; / are done;
		expand; /done
		parse; / done 
		syntax errors 
		signals;
		norminette;

		builtsins;
		export;
		execution;
		herdoc;